ext {
    // Magisk Module
    magiskModuleId = "voidwalker"
    moduleName = "voidwalker"
    moduleAuthor = "electrondefuser"
    moduleDescription = "Walk the shadows of the kernel, remain undetected by the Watch"
    moduleVersion = "v1.10.0"
    moduleVersionCode = 13

    // Frida
    fridaVersion = "17.4.0"
}
